import 'package:flutter/material.dart';

class ProductListTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('List under construction <3'),
      ),
    );
  }
}
